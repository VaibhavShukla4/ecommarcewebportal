/** @format */

import { Inter, Poppins } from 'next/font/google';

export const poppins = Poppins({
  display: 'swap',
  weight: '600',
  subsets: ['latin'],
});

export const inter = Inter({
  display: 'swap',
  weight: '600',
  subsets: ['latin'],
});